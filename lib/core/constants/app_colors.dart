import 'package:flutter/material.dart';

/// Màu sắc chủ đạo của ứng dụng HanzifyPro
class AppColors {
  AppColors._();

  // Màu chủ đạo
  static const Color primary = Color(0xFF1A56A4);
  static const Color primaryLight = Color(0xFF2563EB);

  // Màu nền
  static const Color background = Color(0xFF0F0F0F);
  static const Color surface = Color(0xFF1E1E2E);

  // Màu chữ
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9CA3AF);

  // Màu viền
  static const Color border = Color(0xFF2D2D3D);
  static const Color borderFocus = Color(0xFF1A56A4);

  // Màu lỗi
  static const Color error = Color(0xFFEF4444);

  // Màu đỏ truyền thống Hán ngữ
  static const Color chineseRed = Color(0xFFE53935);
}
