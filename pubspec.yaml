name: hanzify_pro
description: "HanzifyPro — Ứng dụng học Hán ngữ thông minh với AI Gemini, Flashcard SRS và hướng dẫn nét viết."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # UI cơ bản
  cupertino_icons: ^1.0.8

  # AI Gemini — Tích hợp Gemini API của Google
  google_generative_ai: ^0.4.7

  # State Management — Riverpod 2.x
  flutter_riverpod: ^2.6.1

  # Local Database — Lưu trữ flashcard offline
  hive_flutter: ^1.1.0

  # Routing — Điều hướng màn hình
  go_router: ^14.6.3

  # Text-to-Speech — Phát âm tiếng Trung
  flutter_tts: ^4.2.0

  # HTTP Client — Gọi API bên ngoài
  dio: ^5.8.0+1

  # Chart — Biểu đồ tiến trình học tập
  fl_chart: ^0.70.2

  # Firebase — Cloud backend
  firebase_core: ^3.13.1
  firebase_auth: ^5.5.4
  cloud_firestore: ^5.6.9

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

  # Code Generation — Sinh TypeAdapter cho Hive
  build_runner: ^2.4.15
  hive_generator: ^2.0.1
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Khai báo thư mục assets
  assets:
    - assets/hsk4_vocab.json
    - assets/data/
    - assets/app_icon.png

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/app_icon.png"
  min_sdk_android: 21
